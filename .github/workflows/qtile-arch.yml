name: build-qtile-arch

on:
  workflow_dispatch:

env:
  PKGDEST: "/tmp"

jobs:
  build:
    runs-on: ubuntu-latest
    container: archlinux:latest    
    steps:
      - name: Checkount repo
        uses: actions/checkout@v3    
      - name: Prepare for makepkg
        run: |
          pacman -Syu --noconfirm base-devel sudo git
          useradd builder -m -G wheel && echo "builder ALL=(ALL) NOPASSWD: ALL" >> /etc/sudoers
          git clone https://gitlab.archlinux.org/archlinux/packaging/packages/qtile.git
          chown builder -R ..
          chown builder -R /tmp
          su builder -c "gpg --recv-keys 1F713A69C898ED88"
          cd qtile
          sed -i '/cd $pkgname/a \ \ curl -L https://raw.githubusercontent.com/elParaguayo/qtile/fe69b8f8753d2b625f97d2abf1c2b2b16e47b9db/libqtile/widget/statusnotifier.py -o ./libqtile/widget/statusnotifier.py' PKGBUILD
          sed -i '/cd $pkgname/a \ \ patch -Np1 -i ../../../libinput_ffi_build.patch' PKGBUILD
#          sed -i '/cd $pkgname/a \ \ patch -Np1 -i ../../../inputs.patch' PKGBUILD
      - name: Build
        run: |
          cd qtile
          su builder -c "yes '' | MAKEFLAGS=\"-j $(nproc)\" makepkg --noconfirm -sc" 
      - uses: actions/upload-artifact@v3
        with:
          name: qtile
          path: ${{ env.PKGDEST }}/*.pkg.tar.zst
        
